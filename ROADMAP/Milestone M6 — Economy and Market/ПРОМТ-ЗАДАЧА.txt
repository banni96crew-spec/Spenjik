РОЛЬ

Ты senior Godot 4.6.2 architect и coding-agent в репозитории “The Turf” / “Передел”.

Работай только над M6 — Economy and Market.
Не переходи к M7+.

КОНТЕКСТ

Перед изменениями читай только минимально нужное:

res://docs/prd/06_ECONOMY_AND_MARKET.md — owner PRD;
02_CORE_LOOP_AND_PHASES.md;
03_IDS_AND_CONSTANTS.md;
04_GAME_STATE_SCHEMA.md;
05_CARDS_DATABASE.md;
relevant integration sections в 08, 10, 11, 12, 13;
14_DETERMINISTIC_RANDOM.md;
18_TEST_PLAN.md;
19_IMPLEMENTATION_ORDER.md;
20_LLM_AGENT_RULES.md;
21_OPEN_QUESTIONS_AND_FIXES.md.

Учитывай:

FIX-012: Corrupt Clerk consumes только после successful Status purchase.
FIX-021: market использует unified state["random"].
FIX-022: Income dice потребляют ровно 2 steps.
FIX-025: District Control rebuild — отдельное действие.

M6 реализует только economy-owned behavior.
Не реализуй Roles, Contracts, Debts, Contacts, Turf Levels или AI.

ЗАДАЧА
Проверь repository state и не трогай unrelated user changes.
Точечно проверь M0–M5 prerequisites без глубокого аудита.
Составь компактную matrix: rule → owner → API → mutation → test.
Выяви только прямые integration gaps с future modules.
Unresolved conflicts оформи как OQ-*.
Реализуй required M6 production files.
Добавь focused GUT tests.
Запусти M6 tests, M0–M5 regressions и static scans.
Проверь mutation safety, random steps, Resource immutability, scope и diff.
OUTPUT FILES

Production:

res://logic/economy/PriceLogic.gd
res://logic/economy/MarketLogic.gd
res://logic/economy/IncomeLogic.gd
res://logic/economy/PurchaseValidator.gd
res://logic/economy/PurchaseResolver.gd

Tests:

res://tests/unit/test_price_logic.gd
res://tests/unit/test_market_logic.gd
res://tests/unit/test_income_logic.gd
purchase/rebuild tests в existing M6 suites или отдельных focused files.

Не создавай economy mega-file.
Каждый .gd файл < 250 строк.

REQUIRED API
PriceLogic.gd
full card price preview;
Informant/Laundry scaling;
protected Nal;
minimum-price clamp;
rebuild preview;
modifier pipeline.
static func get_informant_price(player: Dictionary) -> int
static func get_laundry_price(player: Dictionary) -> int
static func get_protected_nal(accountants: int) -> int
static func clamp_price(price: int, min_price: int = 1) -> int
MarketLogic.gd
static func generate_market(state: Dictionary) -> Dictionary
static func can_buy_card(state: Dictionary, player_id: String, card_id: String) -> Dictionary
static func buy_card(state: Dictionary, player_id: String, card_id: String) -> Dictionary
static func rebuild_district_control(state: Dictionary, player_id: String) -> Dictionary
static func get_rotating_slot_count(turf_level: int) -> int
IncomeLogic.gd
resolve income для одного player;
optional orchestration для 4 players;
Cops upkeep;
get_cops_upkeep_interval(state, player).
PurchaseValidator.gd

Read-only purchase/rebuild validation.

PurchaseResolver.gd

State placement, Nal/VP changes и modifier consumption только после success.

Internal signatures согласуй с existing patterns.
Не создавай competing public APIs.

PRICE CONTRACT

Используй M2 card catalogs/definitions.
Не дублируй mutable price table.
Не парси display text / effect_summary.

Scaling:

Informant: 5 / 6 / 7 при owned 0 / 1 / 2+.
Laundry: 8 / 10 / 12 при owned 0 / 1 / 2+.

Protected Nal:

0 Accountants → 0;
1 → 4;
2 → 6;
N > 2 → 6 + (N - 2).

Modifier order:

Base/scaled price.
Rebuild override.
Role hook.
Turf hook.
Temporary/contact modifiers.
Clamp к minimum 1.

Price preview не мутирует state, random, flags, modifiers или Resources.

Role/Turf modules ещё не реализованы.
Создай clean integration contract без полной логики M8/M12.

MARKET CONTRACT

Always available:

informant;
stash;
thug;
cops.

Rotating pool:

laundry;
accountant;
brothel;
workshop;
district_control;
cartel;
judge;
bruiser;
cleaner;
insider;
saboteur;
federal_raid.

Rules:

pools не пересекаются;
IDs valid и unique;
rotating slots: 4 ниже Turf Level 4, иначе 3;
selection через SeededPicker.pick_unique;
shared state["random"] обновляется exact selected count steps;
same initial state → same market;
market соответствует M4 schema.
PURCHASE CONTRACT

Validation проверяет:

Market phase;
valid state/player/card;
availability;
enough Nal по final price;
one copy same card_id per player per round;
card-specific requirements/limits.

Use documented errors only:

INVALID_PHASE;
INVALID_PLAYER_ID;
INVALID_CARD_ID;
CARD_NOT_AVAILABLE_IN_MARKET;
NOT_ENOUGH_NAL;
CARD_ALREADY_PURCHASED_THIS_ROUND;
REQUIREMENT_NOT_MET;
CARD_LIMIT_REACHED;
INVALID_STATE.

Missing error = PRD/OQ issue, not ad-hoc string.

Card rules:

District Control требует district_control < workshop;
Accountant base requirement: vp >= 1; Gray Cardinal bypass — M8 hook;
Brothel / active Cops нельзя купить повторно;
active Cartel/Judge нельзя купить повторно;
depleted Cartel можно reactivate.

Success:

Subtract final price.
Place card by Resource type/destination.
Status gives documented VP.
Add card_id to purchased_this_round.
Consume matching modifier.
Validate final state.

Placement:

Engine → engine fields;
Status → counters + 1/2/3 VP;
Defense → Cops/Cartel/Judge state;
War → hand.

Failure keeps Nal, random, cards, flags, modifiers deep-equal unchanged.

FIX-012:

Corrupt Clerk modifier visible in preview;
consumes only after successful Status purchase;
failed/non-Status purchase does not consume.

Если M11 marker unclear — create OQ-* and leave explicit hook.

INCOME CONTRACT

Order per player:

+2 Nal per Laundry.
SeededRandom.roll_d6_pair.
+1 Nal per Informant.
Brothel +5 on doubles.
Black Cash changes bonus to +6.
Add total Nal.
Process Cops upkeep.
Debt hook.
Contract hook.
Validate result.

Random:

exactly 2 steps per resolved player;
updated random state saved;
preview does not roll dice;
failure before roll consumes no random.

Black Cash: check only documented contact state.
Do not implement ContactLogic.

Cops:

active timer increments after income;
interval 3;
human interval 2 at Turf Level 5+;
AI interval stays 3;
due + Nal >= 1: pay 1, timer 0, active;
due + Nal < 1: timer 0, inactive;
Nal never negative.

Debt/Contract modules are not implemented.
Do not fake-stub penalties/progress.
If compile-time dependency blocks work — create OQ-*.

REBUILD CONTRACT
Dedicated action, not fake card_id.
Market phase only.
Requires can_rebuild_district_for_8 == true.
Default price 8.
Future District Boss price 7 via role hook.
Still requires district_control < workshop.
Success: subtract price, +1 District Control, +3 VP, flag false.
Failure: no mutation.
STARTING RESOURCES

Base:

STARTING_NAL = 5;
STARTING_VP = 0.

Provide base/setup helper or documented hook required by 19_IMPLEMENTATION_ORDER.md.

Do not:

implement role/Turf starting effects;
choose strong AI;
change M4 Factory unless necessary and justified.
MINIMUM TESTS

Cover:

price scaling, protected Nal, modifier order, minimum 1;
preview no mutation/no random;
deterministic unique market, slot count, exact steps;
purchase errors and all placement categories;
failed purchase no mutation/consumption;
Income calculations and exact +2 random steps;
Cops intervals, payment, deactivation;
rebuild success/failures;
future hooks not replaced by gameplay logic.
STRICT LIMITS
Do not start M6 if prerequisite gate fails.
Do not change prices, market pools, card effects.
Do not parse Resource display text.
Do not mutate Resources.
Do not use forbidden random APIs.
Do not consume random in preview/validation.
Do not mutate original state on failure.
Do not implement CombatEngine.
Do not implement RoleLogic, ContractLogic, DebtLogic, ContactLogic, TurfLevelLogic.
Do not implement AI purchase decisions.
Do not create GameStateManager or UI.
Do not add save/load, web stack, hidden global state.
Do not weaken tests.
Do not hide failed/not-run checks.
Do not revert unrelated user changes.
Do not leave ambiguity without OQ-*.
WORK ORDER
Check prerequisites, repo state, fixes, open questions.
Build compact matrices for price, market, purchase, income, rebuild.
List M8–M13 boundaries.
Give a short plan without code and wait for my ок.
After ок, update fixtures and focused tests first.
Implement PriceLogic + PurchaseValidator/Resolver.
Implement MarketLogic.
Implement IncomeLogic + Cops upkeep.
Implement rebuild.
Run M6 tests, M0–M5 regressions, scans.
Verify mutation snapshots, random steps, Resources, file length, scope, diff.
RESPONSE FORMAT

First response: plan only, no code, no file changes.

After my ок:

work directly in repo;
use res://... paths;
report failed/not-run checks honestly;
do not mark M6 complete if gate fails.

Final report:

Prerequisites:
- M0–M5: PASS/FAIL/NOT RUN

Implemented:
- ...

Changed files:
- res://...

Integration boundaries:
- ...

Tests and scans:
- command
- passed/failed

Open questions:
- none / OQ-*

M6 gate:
- PASS/FAIL/NOT RUN
M6 GATE CHECKLIST
 M0–M5 gates pass.
 All 5 production files exist.
 Prices/scaling/protected Nal correct.
 Price preview does not mutate state/random.
 Market deterministic, unique, shared-random based.
 Purchases validate and place cards correctly.
 Failed purchases do not mutate state/flags/modifiers.
 Income dice deterministic and use 2 steps/player.
 Cops upkeep works.
 District Control rebuild works as dedicated action.
 Future hooks do not implement foreign modules.
 Regressions and static scans pass.
 All .gd files < 250 lines.
 No M7+ gameplay or web stack added.
 User changes preserved.